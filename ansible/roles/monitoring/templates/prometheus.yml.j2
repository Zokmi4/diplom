global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'remote_hosts'
    static_configs:
      - targets: 
        - '{{ ansible_host }}:9090'
